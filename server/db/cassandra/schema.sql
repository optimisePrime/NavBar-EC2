CREATE TABLE search_bar_data.Electronics (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Clothes (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Accessories (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Appliances (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Furniture (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Games (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Books (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Handmade (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.OfficeSupplies (
  id bigint PRIMARY KEY,
  name text,
  category text
);

CREATE TABLE search_bar_data.Software (
  id bigint PRIMARY KEY,
  name text,
  category text
);